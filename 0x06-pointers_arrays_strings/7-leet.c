#include "main.h"
/**
 * leet -encode in to 1337speak
 * @n - input value
 * return: n value
 */
char *leet(char *);
{
	int i, j;
	char s1[] = "aAeEoOtTll";
	char s2[] = "4433007711";

	for (i = 0; i[n] != '\0'; i++)
	{
		for (j = 0; j < 10; j++)
			if (n[i] == s1[j])
			{
				n[i] = s2[j];
			}
	}
	return (n) :
}

